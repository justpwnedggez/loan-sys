import App from '../App';

export default function Roles() {
    return (
        <div>
            <h1>Roles Master File</h1>
        </div>
    );
}

Roles.layout = (page) => <App children={page} />;
