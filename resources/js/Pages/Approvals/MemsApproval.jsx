import App from '../App';

export default function MemsApproval() {
    return (
        <div>
            <h1>Memberships Approvals</h1>
        </div>
    );
}

MemsApproval.layout = (page) => <App children={page} />;
