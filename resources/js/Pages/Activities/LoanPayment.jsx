import App from '../App';

export default function LoanPayment() {
    return (
        <div>
            <h1>Loan Payment Activity</h1>
        </div>
    );
}

LoanPayment.layout = (page) => <App children={page} />;
