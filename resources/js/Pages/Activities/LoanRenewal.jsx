import App from '../App';

export default function LoanRenewal() {
    return (
        <div>
            <h1>Loan Renewal Activity</h1>
        </div>
    );
}

LoanRenewal.layout = (page) => <App children={page} />;
