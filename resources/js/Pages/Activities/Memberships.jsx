import App from '../App';

export default function Memberships() {
    return (
        <div>
            <h1>Memberships Activity</h1>
        </div>
    );
}

Memberships.layout = (page) => <App children={page} />;
